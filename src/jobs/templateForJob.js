// require's:

// CONST's (hardcoded variables):

// call modules with above CONST'S as their respective arguments:
    // makeLyMediaList(searchParams1-x); // returns an array of IDs
    // singleItemRemoveTextFromDescription(changeParams1-n);